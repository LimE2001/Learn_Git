"master" "develop" "feature"


git add .
git commit -m "..."
git push origin LimE


git checkout -b LimE 	//tạo branch
git checkout LimE 	//chuyển branch
git branch -D LimE	//xóa branch local
git push --delete LimE


git pull origin LimE
git diff		// xem những thay đổi


git innit
git add .
git commit -m "..."
git remote add /link res/
git push origin LimE


git checkout master 
git merge LimE
